<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sharad.epocket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Dashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:noHistory="true"></activity>
        <activity
            android:name=".budget.CategoryActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".cards.AddCardActivity"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".accounts.AccountTransactionsActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".accounts.AddTransactionActivity"
            android:label=""
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.sharad.epocket.MainActivity" />
        </activity>
    </application>

</manifest>